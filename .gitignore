# Everything under data/ is crawled, derived or generated. The JSONL and images
# are durable catalogue state, but they are large and belong in a backup, not in
# git; the database and caches are rebuildable from them.
data/

__pycache__/
*.pyc
.venv/
.DS_Store
